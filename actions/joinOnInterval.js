

module.exports = {
    joinAllActiveServersOnInterval: async (client) => {
        client.guilds
        // .filter(guild => ) // TODO: is it an active server? database query
        .forEach(guild => {

        })
    }
}